import styled from "styled-components";

const Loading = () => {
  return <BoxLoading>Loading...</BoxLoading>;
};

export default Loading;

const BoxLoading = styled.p`
  text-align: center;
  margin-top: 200px;
  font-weight: 500;
  font-size: 20px;
`;
